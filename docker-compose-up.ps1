# change directory to the backend folder
cd .\backend

# start the backend services using docker-compose
docker-compose up -d

# change directory to the frontend folder
cd ..\frontend

# start the frontend services using docker-compose
docker-compose up -d